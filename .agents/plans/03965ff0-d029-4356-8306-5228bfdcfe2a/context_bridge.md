- **📍 Current State & Focus**: A finalized implementation plan has been written and approved by the user. The project focus is stripping away legacy `yieldCallback` mechanisms from the FreeRTOS multitasking architecture since all network fetching now relies natively on `DataWorker` and `vTaskDelay(1)`.
- **🎯 Next Immediate Actions**: The user will commit this plan to git and migrate to a machine capable of running `pio run`. Once migrated and `/plan-load` -> `/plan-queue` are executed, claim the lock and execute the modifications outlined in `implementation_plan.md`. This largely involves deleting dead function pointers and callback overrides across `appContext`, `weatherClient`, `rssClient`, `tflDataSource`, and `nationalRailDataSource`, followed by modifying multiple markdown files in `docs/reference`.
- **🧠 Decisions & Designs**: Validated that `yieldCallbackWrapper` was effectively dead code due to explicit `pcTaskGetName` overrides that prevented the Core 0 background worker from interfering with Core 1 hardware locking.
- **🐛 Active Quirks, Bugs & Discoveries**: We are intentionally holding execution here on this machine since PlatformIO is not available. The next agent dropping in will assume it has standard compilation tools available before executing the codebase modifications.
- **💻 Commands Reference**: Standard validation via `pio run` is required after interface removal.
- **🌿 Execution Environment**: Standard ESP32 IDF dual-core environment. Awaiting migration to a host machine with PlatformIO installed.
